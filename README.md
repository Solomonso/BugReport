### **Bug Report**

A simple web page used for storing software bug reports in MYSQL database. it includes fields such as product name, version and proposed solution and the time the bug was reported.

The main page shows the bug reported in a table and it's possible to add, edit and delete the bugs reported.

Note: run the create_db.php first.